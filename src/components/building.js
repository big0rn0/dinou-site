import React from 'react'
import Building from "gatsby-image";

const Header = props => (
  <div>
    <Img
      title="Under construction"
      alt="Din Ou's almost there"
      sizes={props.headerImage.sizes}
    />
  </div>
);

export default Building
